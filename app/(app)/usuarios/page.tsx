"use client"
import{toast}from"sonner";import{useSigia}from"@/lib/store";import{ROLES,DEPARTMENTS,type Role}from"@/lib/types";import{PageHeader}from"@/components/page-header";import{Card,CardContent}from"@/components/ui/card";import{Table,TableBody,TableCell,TableHead,TableHeader,TableRow}from"@/components/ui/table";import{Select,SelectContent,SelectItem,SelectTrigger,SelectValue}from"@/components/ui/select";import{Badge}from"@/components/ui/badge"
export default function Page(){const{users,currentUser,updateUser}=useSigia();const admin=currentUser?.role==="administrador";const save=async(id:string,patch:any)=>{try{await updateUser(id,patch);toast.success("Usuario actualizado")}catch(e:any){toast.error(e.message)}};return <div className="space-y-6"><PageHeader title="Usuarios" description="Las cuentas se registran con Supabase Auth; el administrador gestiona rol y departamento aquí."/><Card><CardContent className="p-0"><Table><TableHeader><TableRow><TableHead>Nombre</TableHead><TableHead>Rol</TableHead><TableHead>Departamento</TableHead><TableHead>Estado</TableHead></TableRow></TableHeader><TableBody>{users.map(u=><TableRow key={u.id}><TableCell className="font-medium">{u.name}</TableCell><TableCell>{admin?<Select value={u.role} onValueChange={v=>save(u.id,{role:v as Role})}><SelectTrigger className="w-40"><SelectValue/></SelectTrigger><SelectContent>{ROLES.map(r=><SelectItem key={r.value} value={r.value}>{r.label}</SelectItem>)}</SelectContent></Select>:u.role}</TableCell><TableCell>{admin?<Select value={u.department||"TI"} onValueChange={v=>save(u.id,{department:v})}><SelectTrigger className="w-48"><SelectValue/></SelectTrigger><SelectContent>{DEPARTMENTS.map(d=><SelectItem key={d} value={d}>{d}</SelectItem>)}</SelectContent></Select>:u.department}</TableCell><TableCell><Badge variant={u.status==="activo"?"secondary":"outline"}>{u.status}</Badge></TableCell></TableRow>)}</TableBody></Table></CardContent></Card></div>}
